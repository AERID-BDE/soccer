services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5174:5174"
    volumes:
      # Mount source code for hot reload
      - .:/app
      - /app/node_modules
      # Mount public folder for data persistence
      - ./public:/app/public
    environment:
      - VITE_GOOGLE_CLIENT_ID=${VITE_GOOGLE_CLIENT_ID:-}
    stdin_open: true
    tty: true

